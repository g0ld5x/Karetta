#ifndef SEMANALYZER_H
#define SEMANALYZER_H
#include <string>
#include <vector>
#include <variant>
#include <memory>

enum class PrimitiveTypes {
    Integer,
    Double,
    Char,
    String,
    Bool
};

#endif